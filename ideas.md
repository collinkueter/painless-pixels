Allow the user to not only move the text, but also the frame/image altogether, they should be able to place anywhere on the image. Also show all images on the canvas next to each other so the user gets an idea of how it will show in the appstore. Similar to how appscreens does it as in the attached picture. Also allow the user to rotate their text or frame/image within the screenshot and set the angle to a degree to create consistency. 